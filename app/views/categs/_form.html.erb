<%= form_for(@categ) do |f| %>
  <% if @categ.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categ.errors.count, "error") %> prohibited this categ from being saved:</h2>

      <ul>
      <% @categ.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
